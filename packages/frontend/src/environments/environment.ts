/**
 * Environment configuration
 * Demonstrates: Configuration management, environment-specific settings
 */

export const environment = {
  production: false,
  apiUrl: '/api',
  wsUrl: `ws://${window.location.hostname}:3003`,
};

